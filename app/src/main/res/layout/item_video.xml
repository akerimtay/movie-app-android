<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="video"
            type="com.akerimtay.movieapp.data.model.Video" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="@dimen/item_video_width"
        android:layout_height="@dimen/item_video_height"
        app:cardCornerRadius="@dimen/radius_value">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:foreground="?android:selectableItemBackground">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                app:loadImage="@{video.buildYouTubeThumbnailURL()}"
                app:placeholder="@{@drawable/placeholder_poster}"
                tools:background="@tools:sample/backgrounds/scenic"
                tools:ignore="ContentDescription" />

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/video"
                android:src="@drawable/icon_youtube"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/rect_shadow_gradient_270"
                app:layout_constraintBottom_toBottomOf="parent">

                <TextView
                    android:id="@+id/txt_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginHorizontal="@dimen/small_margin"
                    android:ellipsize="end"
                    android:fontFamily="@font/open_sans"
                    android:gravity="center"
                    android:lines="1"
                    android:paddingVertical="@dimen/super_small_margin"
                    android:text="@{video.name}"
                    android:textColor="@color/white_100"
                    android:textSize="@dimen/medium_text_size"
                    tools:text="@tools:sample/lorem/random" />

            </LinearLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>

</layout>